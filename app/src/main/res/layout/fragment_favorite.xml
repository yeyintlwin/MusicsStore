<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/fragment_favorite_swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <android.support.v7.widget.RecyclerView
                android:id="@+id/fragment_favorite_recyclerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone"/>
        <include
                android:id="@+id/fragment_favorite_empty"
                layout="@layout/layout_empty"
                android:visibility="gone"/>
        <include
                android:id="@+id/fragment_favorite_offline"
                layout="@layout/layout_offline"
                android:visibility="gone"/>
        <include
                android:id="@+id/fragment_favorite_loading"
                layout="@layout/layout_loading"
                android:visibility="gone"/>
    </RelativeLayout>
</android.support.v4.widget.SwipeRefreshLayout>